import React from "react";
import { FC } from "react";
import { Alert } from "react-bootstrap";

const SuccessAlert: FC = () => {
  return <Alert variant="success">Your order successfully done!</Alert>;
};

export default SuccessAlert;
